from typing import Any, Dict


def publish_with_selenium(payload: Dict[str, Any]) -> None:
    raise NotImplementedError(
        "Naver Blog has no official public posting API. Implement Selenium/Playwright automation with login, cookie/session persistence, and DOM interactions."
    )